name: logs-grpc-server
description: Logs GRPC server
owner: DevAppSys

server:
  port: 50051

kafka:
  brokers:
    - "kafka:9092" 
  buffer_channel_size: 10485760
  retry_attempts: 3
  write_timeout: 10
  read_timeout: 10

  topics:
    logs: "logs"
    events: "events"
    context: "context"